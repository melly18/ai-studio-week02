### 1. 상품코드를 입력하면 즉시 재고 수량을 확인해야 하는 재고 관리 데이터
{상품코드 : 재고 수량} 형태의 dictionary가 가장 유용해 보인다. 상품 코드를 key로 접근하면 재고 수량을 바로 알 수 있기 때문이다. list와 set의 경우, 상품 코드를 찾기 위해서 자료구조를 순회해야 하기 때문에 시간이 소요되고, 재고 수량은 늘 변하기 때문에 tuple을 사용하는 것도 부적절하다.

```python
storage = {"A-1004" : 34, "B-987" : 5, "U-007" : 108}
print(storage["A-1004"])
storage["A-1004"] -= 1
print(storage["A-1004"])
```
<br>

### 2. 이벤트 응모 고객 명단에서 중복 응모를 제거하고, 기존 회원 명단과의 교집합을 구해야 하는 상황
중복된 항목을 제거해야 하기 때문에 set이 가장 적절하다. list와 tuple은 중복을 제거하기 위해서는 별도의 번거로운 작업이 필요하고, dictionary의 경우 key값이 중복될 수 없음을 이용할 수도 있겠지만 현재 상황에서 명단을 {key : value} 형식으로 나타내는 건 불필요하기 때문이다. 따라서 이벤트 응모 고객을 set으로 나타내고 기존 회원 명단 또한 set으로 만들어준 후, & 연산자를 통해 중복 원소를 찾아내면 된다.

```python
members = {"김철수", "이영희", "박서강", "정강서", "John Mayers", "주대학"}
events = set()
events.update(["이영희", "박서강", "홍길동", "홍길동", "박서강", "Jane Harris", "유서강", "홍길동", "권수철", "정희영", "James Doe", "윤기역", "정서강", "이영희"])
print(f"이벤트 참여자: {events}")
print(f"기존 회원 참여자: {members & events}")
```
<br>

### 3. 월별 매출액을 1월부터 12월까지 순서대로 저장하고 순회하는 데이터
list가 가장 적절할 듯하다. 우선 순서가 없는 set은 제외한다. 나머지 list, tuple, dictionary는 모두 순서가 있는데, dictionary의 경우 key값을 설정하여 value에 이름을 붙일 수 있다는 장점이 있지만 1월부터 12월까지 연속된 데이터기 때문에 굳이 필요하지 않다. 순회할 때도 일일이 key값을 입력해야 해서 오히려 이 상황에서는 불편할 수 있다. 인덱스를 통해 0~11까지를 각각 1월부터 12월까지라고 받아들이면 된다. 따라서 list와 tuple 모두 가능한데, tuple의 경우 한 번 생성한 후 수정이 불가하기 때문에 1월에 기록한 자료구조에 2월 데이터를 추가하지 못하는 일이 벌어질 수 있다. 그러므로 수정도 가능하고 순서가 있는 list가 적합하다.

```python
month_sales = [100, 200, 150, 120, 340, 260, 110, 180, 290, 170, 105]
month_sales.append(370)
for month, sales in enumerate(month_sales):
    print(f"{month+1}월 매출: {sales:,}만 원")
```
<br>

### 4. 한 번 발급되면 절대 변경되어서는 안 되는 (위도, 경도) 매장 좌표
절대 변경되어서는 안 되기 때문에 tuple을 사용해야 한다. 나머지 dictionary, list, set은 모두 자료구조 선언 이후 데이터의 수정이 가능하기 때문에 보안을 위해서 선택할 수 없다. 

```python
location = ("북위 37도", "동경 131도")
location[0] = "북위 21도" #Error!!
location[1] = "동경 40도" #Error!!
```
<br>

### 5. 1,000만 줄짜리 웹 서버 접속 로그 파일에서 특정 조건의 줄 수를 세는 작업
1,000만 줄이라는 대형 데이터를 list, dictionary, tuple, set과 같은 자료구조에 불러오려 한다면 Out of Memory Error가 일어날 수 있다. 따라서 한 번에 데이터를 올리는 것이 아니라 필요할 때 하나씩 꺼내 쓸 수 있도록 generator를 사용해야 한다. 1,000만 줄의 웹 서버 접속 로그 파일을 연 다음, 반복문을 통해 한 줄씩 읽어 접근한다. if문 등을 통해 특정 조건을 만족하는지 파악하고 수를 센다면 memory error 없이 문제를 해결할 수 있다.

```python
count = 0
with open("web_log.csv", encoding="utf-8") as f:
    for line in f:
        if "404" in line:
            count += 1
```